var obj = payload.DeepDetect.objects;
if(objs.length === 0) {
  print('No object detected');
}else{
  for (var i = 0; i<objs.length; i++){
  var MachineID = '<Machine Name>';
  payload = {MachineID : '<Machine Name>'};
  print('Status : ', MachineID)
  }
}
